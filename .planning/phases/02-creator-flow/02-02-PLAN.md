---
phase: 02-creator-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - client/src/pages/CreatePage.jsx
  - client/src/components/create/SuccessState.jsx
  - client/src/index.css
  - server/services/wishService.js
autonomous: true
requirements:
  - PHOTO-01
  - PHOTO-02
  - THEME-01
  - WISH-03

must_haves:
  truths:
    - "User fills the form, drag-drops photos, selects a theme, and clicks Create Wish"
    - "After creation, user sees an inline success state with a compact preview card, Copy Link button, and Open Link button"
    - "Inline validation errors appear near required fields when form is submitted with empty/invalid values"
    - "Sender name field is optional -- user can create a wish anonymously"
    - "Server accepts wishes with empty senderName"
  artifacts:
    - path: "client/src/pages/CreatePage.jsx"
      provides: "Refactored form using PhotoUploader, ThemeSelector, inline validation"
      exports: ["default"]
    - path: "client/src/components/create/SuccessState.jsx"
      provides: "Compact preview card with copy/open link buttons"
      exports: ["default"]
    - path: "server/services/wishService.js"
      provides: "Updated createWish with senderName optional"
      contains: "recipientName"
  key_links:
    - from: "client/src/pages/CreatePage.jsx"
      to: "PhotoUploader"
      via: "import and render <PhotoUploader>"
      pattern: "import PhotoUploader"
    - from: "client/src/pages/CreatePage.jsx"
      to: "ThemeSelector"
      via: "import and render <ThemeSelector>"
      pattern: "import.*ThemeSelector"
    - from: "client/src/pages/CreatePage.jsx"
      to: "SuccessState"
      via: "conditional render when createdWish is set"
      pattern: "createdWish.*SuccessState"
    - from: "client/src/pages/CreatePage.jsx"
      to: "POST /api/wish"
      via: "fetch in generate()"
      pattern: "fetch.*api/wish"
    - from: "server/services/wishService.js"
      to: "Prisma Wish model"
      via: "prisma.wish.create"
      pattern: "prisma.wish.create"
---

## Phase Goal

**As a** wish creator, **I want to** complete the full creation flow -- fill the form, upload photos, pick a theme, and see a success preview with shareable link -- **so that** I can create and share a birthday wish in one seamless experience.

## Acceptance Criteria

- [ ] CreatePage renders PhotoUploader component for photo selection (per D-05, D-06)
- [ ] CreatePage renders ThemeSelector component with color circles (per D-08, D-09, D-10)
- [ ] Inline validation errors display near required fields on submit (per D-11, D-12)
- [ ] Sender name is optional -- no validation error when blank (per D-13)
- [ ] Server wishService accepts wishes with empty senderName
- [ ] After wish creation, CreatePage shows SuccessState component (per D-01, D-02)
- [ ] SuccessState shows compact preview card (~200px tall) with recipient name, first line of message, theme swatch (per D-03)
- [ ] SuccessState has Copy Link button (clipboard API with execCommand fallback)
- [ ] SuccessState has Open Link button (window.open)
- [ ] "Create another wish" link resets to form view
- [ ] Form-creator-flow skill file created at .claude/skills/form-creator-flow/SKILL.md (per D-14)
- [ ] Old inline photo/theme/success code removed from CreatePage (replaced by components)

## MVP Slice Tasks

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-creator-flow/02-CONTEXT.md
@.planning/phases/02-creator-flow/02-RESEARCH.md
@.planning/phases/02-creator-flow/02-PATTERNS.md
@.planning/phases/02-creator-flow/02-UI-SPEC.md
@.planning/phases/02-creator-flow/02-01-SUMMARY.md
@.claude/skills/photo-upload-security/SKILL.md
@client/src/pages/CreatePage.jsx
@server/services/wishService.js
@client/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor CreatePage with sub-components, inline validation, and optional sender</name>
  <files>
    client/src/pages/CreatePage.jsx,
    client/src/index.css,
    server/services/wishService.js
  </files>
  <read_first>
    client/src/pages/CreatePage.jsx,
    client/src/components/create/PhotoUploader.jsx,
    client/src/components/create/ThemeSelector.jsx,
    server/services/wishService.js,
    client/src/index.css,
    .planning/phases/02-creator-flow/02-CONTEXT.md,
    .planning/phases/02-creator-flow/02-PATTERNS.md,
    .planning/phases/02-creator-flow/02-UI-SPEC.md
  </read_first>
  <action>
    1. **Update server validation** in `server/services/wishService.js` line 28:
    Change `if (!senderName || !recipientName || !message)` to `if (!recipientName || !message)`.
    Update the error message to `"recipientName and message are required"`. This makes senderName optional per D-13.

    2. **Refactor CreatePage.jsx** (the full file will be rewritten with these changes):

    a. **Imports** -- add at top (per CONVENTIONS.md import order):
       - `import PhotoUploader from "../components/create/PhotoUploader.jsx"`
       - `import { THEMES } from "../components/create/ThemeSelector.jsx"` (named import)
       - `import ThemeSelector from "../components/create/ThemeSelector.jsx"` (default import)

    b. **Remove the THEMES constant** (lines 5-11) -- it now lives in ThemeSelector.jsx. Import it instead.

    c. **Remove old photo handling code**: delete `handlePhotoSelect` function (lines 34-42), `removePhoto` function (lines 44-46), and the inline photo JSX section (lines 277-313).

    d. **Replace photo section JSX** (the `form__section` for photos, lines 277-313) with:
       ```
       <PhotoUploader photos={photos} onPhotosChange={setPhotos} maxFiles={5} />
       ```
       Keep the section title "Photos (optional)" wrapping div.

    e. **Replace theme section JSX** (lines 316-338) with:
       ```
       <ThemeSelector value={form.theme} onChange={(id) => update("theme", id)} />
       ```
       Keep the section title "Theme" wrapping div.

    f. **Add inline validation state** (per D-11, D-12, 02-UI-SPEC.md Form Validation Contract, 02-RESEARCH.md Pattern 2):
       - Add state: `const [errors, setErrors] = useState({})`
       - Add state: `const [submitted, setSubmitted] = useState(false)`
       - Add `validate(field, value)` function per 02-RESEARCH.md Pattern 2:
         - recipientName: `value.trim() ? "" : "Their name is required"`
         - month: `value >= 1 && value <= 12 ? "" : "Birth month must be 1-12"`
         - day: `value >= 1 && value <= 31 ? "" : "Birth day must be 1-31"`
         - message: `value.trim() ? "" : "Please write a birthday message"`
       - In `generate()`: replace the throw-based validation (lines 74-88) with validation that populates the `errors` object. Set `submitted: true`. If any errors exist, `return` early (do not throw).
       - On each field's `onChange`: if `submitted` is true and `errors[field]` exists, clear that field's error via `setErrors(prev => ({ ...prev, [field]: "" }))`.

    g. **Make senderName optional** (per D-13):
       - Remove `required` attribute from sender name `<input>` (line 202)
       - Remove the senderName validation from generate() (line 74-76)
       - In the POST body, default senderName: `form.senderName.trim() || ""`

    h. **Add inline error display** below each required field (per 02-UI-SPEC.md):
       - After recipient name input: `{errors.recipientName && <span className="form__field-error">{errors.recipientName}</span>}`
       - After birth month input: same pattern with `errors.month`
       - After birth day input: same pattern with `errors.day`
       - After message textarea: same pattern with `errors.message`
       - Each errored input should get `className` appended with `form__input--error` when its error is truthy
       - Replace single `error` state display (line 341) with `errors.global` for API-level errors

    i. **Replace error state**: change the single `error` string state to use `errors.global` for API-level failures from the catch block. The catch block sets `setErrors(prev => ({ ...prev, global: err.message }))`.

    3. **Add CSS for validation errors** to `client/src/index.css`:
    - `.form__field-error` -- color: #c0392b, font-size: 0.875rem, font-weight: 700, font-family: "Nunito", sans-serif, margin-top: 4px, display: block
    - `.form__input--error` -- outline: 2px solid #c0392b, outline-offset: -1px (overrides the default mint focus ring when in error state)
  </action>
  <verify>
    <automated>grep -c "PhotoUploader" /mnt/d/Programming/VibeCode-Tour/birthday-wish-generator/client/src/pages/CreatePage.jsx && grep -c "ThemeSelector" /mnt/d/Programming/VibeCode-Tour/birthday-wish-generator/client/src/pages/CreatePage.jsx && grep -c "form__field-error" /mnt/d/Programming/VibeCode-Tour/birthday-wish-generator/client/src/index.css && grep "recipientName and message" /mnt/d/Programming/VibeCode-Tour/birthday-wish-generator/server/services/wishService.js</automated>
  </verify>
  <acceptance_criteria>
    - CreatePage.jsx imports PhotoUploader and ThemeSelector from components/create/
    - CreatePage.jsx no longer contains the inline THEMES constant (imported from ThemeSelector.jsx)
    - CreatePage.jsx no longer contains handlePhotoSelect, removePhoto functions (replaced by PhotoUploader)
    - CreatePage.jsx no longer contains inline photo preview JSX (form__photo-preview, form__photo-thumb)
    - CreatePage.jsx no longer contains inline theme button JSX (form__themes, form__theme-btn)
    - CreatePage.jsx has errors state object and submitted state boolean
    - CreatePage.jsx has validate(field, value) function checking recipientName, month, day, message
    - CreatePage.jsx displays .form__field-error spans below each required field
    - CreatePage.jsx senderName input does NOT have required attribute
    - server/services/wishService.js line 28 checks only recipientName and message (not senderName)
    - client/src/index.css contains .form__field-error and .form__input--error classes
  </acceptance_criteria>
  <done>
    CreatePage.jsx uses PhotoUploader and ThemeSelector sub-components. Inline validation errors appear near required fields on submit. Sender name is optional on both client and server. Old inline photo/theme code is removed. CSS for validation errors is added.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SuccessState component, integrate into CreatePage, and create form-creator-flow skill</name>
  <files>
    client/src/components/create/SuccessState.jsx,
    client/src/pages/CreatePage.jsx,
    client/src/index.css,
    .claude/skills/form-creator-flow/SKILL.md
  </files>
  <read_first>
    client/src/pages/CreatePage.jsx,
    client/src/index.css,
    .planning/phases/02-creator-flow/02-CONTEXT.md,
    .planning/phases/02-creator-flow/02-PATTERNS.md,
    .planning/phases/02-creator-flow/02-UI-SPEC.md,
    .claude/skills/photo-upload-security/SKILL.md
  </read_first>
  <action>
    1. **Create `client/src/components/create/SuccessState.jsx`** as a default-exported functional component.

    Component contract (per 02-UI-SPEC.md SuccessState Component Contract, 02-CONTEXT.md D-01/D-02/D-03, 02-RESEARCH.md Pattern 4):
    - Props: `wish` (object with id, recipientName, message, theme, photos), `onReset` (function)
    - Local state: `copied` (boolean, false)
    - `getWishUrl()`: returns `${window.location.origin}/wish/${wish.id}`
    - `copyLink()`: async function using `navigator.clipboard.writeText(getWishUrl())` with `document.execCommand("copy")` fallback (copy the exact pattern from CreatePage lines 130-146). Sets `copied=true`, reverts after 2 seconds via `setTimeout`.
    - `openLink()`: calls `window.open(getWishUrl(), "_blank")`
    - Extract first line of message: `wish.message.split(/(?<=[.!?])\s+/)[0] || wish.message`
    - Get theme primary color from THEMES constant: import `{ THEMES }` from `./ThemeSelector.jsx`, find `THEMES.find(t => t.id === wish.theme)?.primary || "#ff6f59"`

    Render structure (per 02-UI-SPEC.md SuccessState visual spec):
    - `success-state` wrapper div (centered text)
    - `success-state__icon` div: green circle with white checkmark (48x48px, use inline CSS for the circle: background var(--mint), border-radius 50%, display flex, align center/justify center, color white, font-size 24px, content "checkmark character or just a styled check")
    - `success-state__heading` h1: "Wish Created!" (Baloo 2, weight 800, 2rem, color var(--mint))
    - `success-state__body` p: "Your birthday wish for" + `<strong>{wish.recipientName}</strong>` + "is ready to share."
    - `success-state__preview` div (~200px tall compact card): border with theme primary color (`style={{ borderColor: themePrimary }}`), contains `success-state__preview-name` (wish.recipientName), `success-state__preview-message` (first line, truncated with CSS text-overflow ellipsis), `success-state__preview-swatch` (small div with theme primary as background color, 24x24px circle)
    - `success-state__link-box` div: cream background box with `<code>{getWishUrl()}</code>` monospace text
    - `success-state__actions` div (flex row): "Open Link" button (`success-state__btn success-state__btn--primary`, coral pill) and "Copy Link" button (`success-state__btn success-state__btn--secondary`, mint-bordered pill, text toggles to "Copied!" for 2 seconds)
    - "Create another wish" text link below (`success-state__back`, mint color, calls `onReset`)
    - Accessibility: heading is `<h1>`, Copy button has `aria-label="Copy wish link to clipboard"`, Open button has `aria-label="Open wish in new tab"`

    2. **Integrate SuccessState into CreatePage.jsx**:
    - Add import: `import SuccessState from "../components/create/SuccessState.jsx"`
    - Replace the existing `if (createdWish)` block (currently rendering inline success JSX, lines 152-185) with:
      ```
      if (createdWish) {
        return (
          <div className="page">
            <SuccessState wish={createdWish} onReset={() => { setCreatedWish(null); setCopied(false); }} />
          </div>
        );
      }
      ```
    - Remove the `copied` state and `copyLink`/`openLink`/`getWishUrl` functions from CreatePage (they now live in SuccessState). Keep `setCopied` reference in onReset if still used, or simplify to just `setCreatedWish(null)`.

    3. **Add CSS for SuccessState** to `client/src/index.css`:
    - `.success-state` -- text-align: center, padding: 48px 28px, display: flex, flex-direction: column, align-items: center, gap: 16px
    - `.success-state__icon` -- width: 48px, height: 48px, border-radius: 50%, background: var(--mint), color: white, display: flex, align-items: center, justify-content: center, font-size: 24px, margin: 0 auto
    - `.success-state__heading` -- font-family: "Baloo 2", cursive, font-size: 2rem, font-weight: 800, color: var(--mint), margin: 0
    - `.success-state__body` -- font-size: 1.05rem, opacity: 0.8
    - `.success-state__preview` -- width: 100%, max-width: 320px, min-height: 120px, border: 2px solid var(--border), border-radius: 12px, padding: 16px, display: flex, flex-direction: column, align-items: center, gap: 8px, text-align: center
    - `.success-state__preview-name` -- font-family: "Baloo 2", cursive, font-size: 1.2rem, font-weight: 700, color: var(--ink)
    - `.success-state__preview-message` -- font-size: 0.9rem, color: var(--ink), opacity: 0.7, max-width: 100%, overflow: hidden, text-overflow: ellipsis, white-space: nowrap
    - `.success-state__preview-swatch` -- width: 24px, height: 24px, border-radius: 50%, border: 1px solid rgba(0,0,0,0.1)
    - `.success-state__link-box` -- background: var(--cream), border: 1px solid var(--border), border-radius: 10px, padding: 10px 16px, max-width: 400px
    - `.success-state__link-box code` -- font-size: 0.85rem, word-break: break-all
    - `.success-state__actions` -- display: flex, gap: 12px, justify-content: center, flex-wrap: wrap
    - `.success-state__btn` -- border-radius: 999px (pill), padding: 12px 24px, font-family: "Baloo 2", cursive, font-weight: 700, cursor: pointer, border: none, font-size: 1rem, transition: background 0.2s, transform 0.1s
    - `.success-state__btn--primary` -- background: var(--coral), color: white
    - `.success-state__btn--secondary` -- background: var(--card), color: var(--mint), border: 2px solid var(--mint)
    - `.success-state__btn--secondary:hover` -- background: var(--mint), color: white
    - `.success-state__back` -- background: none, border: none, color: var(--mint), font-family: "Nunito", sans-serif, font-weight: 700, font-size: 0.9rem, cursor: pointer, margin-top: 8px
    - `.success-state__back:hover` -- text-decoration: underline

    Add responsive rules in the existing `@media (max-width: 480px)` block:
    - `.success-state__actions` -- flex-direction: column, width: 100%
    - `.success-state__btn` -- width: 100%

    4. **Create `.claude/skills/form-creator-flow/SKILL.md`** per D-14.

    Follow the structure of `.claude/skills/photo-upload-security/SKILL.md` as the analog. The skill file should document:
    - YAML frontmatter: name=form-creator-flow, description capturing drag-drop, thumbnails, theme circles, success state, validation
    - Section: react-dropzone drag-drop pattern with cumulative file addition (reference PhotoUploader.jsx pattern, maxFiles dynamic calc, blob URL lifecycle)
    - Section: Thumbnail previews with blob URL management (URL.createObjectURL on selection, URL.revokeObjectURL on removal and unmount)
    - Section: Theme color circle selector (3 circles per theme button, single primary color concept, THEMES constant)
    - Section: Inline success state (no route change per D-01/D-04, compact preview card per D-03, clipboard copy with fallback)
    - Section: Inline field validation (validate on submit, clear on subsequent change, per-field errors object)
    - Section: Memory leak prevention (URL.revokeObjectURL in useEffect cleanup)
    - When Building These Files section listing: PhotoUploader.jsx, ThemeSelector.jsx, SuccessState.jsx, CreatePage.jsx
  </action>
  <verify>
    <automated>grep -c "SuccessState" /mnt/d/Programming/VibeCode-Tour/birthday-wish-generator/client/src/pages/CreatePage.jsx && grep -c "success-state" /mnt/d/Programming/VibeCode-Tour/birthday-wish-generator/client/src/index.css && test -f /mnt/d/Programming/VibeCode-Tour/birthday-wish-generator/.claude/skills/form-creator-flow/SKILL.md && echo "skill exists"</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/create/SuccessState.jsx exists and exports a default function component
    - SuccessState.jsx imports THEMES from ThemeSelector.jsx for theme color lookup
    - SuccessState.jsx contains copyLink function with navigator.clipboard.writeText and execCommand fallback
    - SuccessState.jsx renders compact preview card with recipient name, first message line, theme swatch
    - SuccessState.jsx renders Copy Link and Open Link buttons
    - SuccessState.jsx renders "Create another wish" text link calling onReset prop
    - SuccessState.jsx heading uses <h1> element
    - CreatePage.jsx imports SuccessState and renders it when createdWish is truthy
    - CreatePage.jsx no longer contains inline success JSX (old success__icon, success__link, success__actions code)
    - CreatePage.jsx no longer contains copyLink, openLink, getWishUrl functions (moved to SuccessState)
    - client/src/index.css contains .success-state, .success-state__heading, .success-state__preview, .success-state__btn--primary, .success-state__btn--secondary classes
    - .claude/skills/form-creator-flow/SKILL.md exists with YAML frontmatter containing name: form-creator-flow
    - SKILL.md documents react-dropzone pattern, thumbnail management, theme circles, success state, validation
    - @media (max-width: 480px) block contains .success-state__actions flex-direction: column
  </acceptance_criteria>
  <done>
    SuccessState.jsx is an integrated component showing compact preview card, copy/open link buttons, and "create another" link. CreatePage.jsx renders SuccessState after wish creation, replacing old inline success JSX. The form-creator-flow skill documents all patterns implemented in Phase 2. The full creator flow works end-to-end: fill form, drag-drop photos, pick theme, create wish, see success preview, copy/open link.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-to-api | Form data and photo metadata cross from browser to Express API via POST |
| client-clipboard | Wish URL written to user clipboard via navigator.clipboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | POST /api/wish (senderName now optional) | mitigate | Server still validates recipientName and message. Empty senderName stored as empty string. No injection risk -- Prisma parameterized queries. |
| T-02-04 | Information Disclosure | SuccessState wish URL display | accept | Wish IDs are nanoid(8), unguessable. URL displayed only to the creator, not publicly broadcast. |
</threat_model>

<verification>
- Navigate to /create, fill form with all required fields, drag-drop 2 photos, select "ocean" theme, click "Create Wish"
- Success state appears inline (no navigation): preview card shows recipient name, first message line, ocean blue swatch
- Copy Link copies the wish URL to clipboard
- Open Link opens the wish page in a new tab
- "Create another wish" resets to empty form
- Submit form with empty recipient name: inline error "Their name is required" appears below the field
- Submit form with empty sender name: wish is created successfully (no error)
- Server accepts POST /api/wish with empty senderName field
</verification>

<success_criteria>
- Complete end-to-end creator flow works: form fill, photo drag-drop, theme select, create, success preview, share
- All 16 locked decisions (D-01 through D-16) are respected
- No old inline photo/theme/success code remains in CreatePage.jsx
- Server accepts anonymous wishes (empty senderName)
- form-creator-flow skill file exists and documents all implemented patterns
</success_criteria>

## Artifacts this phase produces

| Symbol | File | Type | Description |
|--------|------|------|-------------|
| `SuccessState` | `client/src/components/create/SuccessState.jsx` | default export (function component) | Compact preview card with copy/open link buttons |
| `CreatePage` (refactored) | `client/src/pages/CreatePage.jsx` | default export (function component) | Refactored form using PhotoUploader, ThemeSelector, inline validation |
| `validate` | `client/src/pages/CreatePage.jsx` | function (internal) | Per-field validation: recipientName, month, day, message |
| `errors` | `client/src/pages/CreatePage.jsx` | state object (internal) | Per-field error strings + global API error |
| `submitted` | `client/src/pages/CreatePage.jsx` | state boolean (internal) | Tracks first submit attempt for error clearing |
| `createWish` (modified) | `server/services/wishService.js` | function (modified) | senderName no longer required in validation check |
| `form-creator-flow` | `.claude/skills/form-creator-flow/SKILL.md` | skill file | Documents drag-drop, thumbnails, theme circles, success state, validation patterns |
| `.success-state__*` CSS classes | `client/src/index.css` | CSS classes (14 classes) | Success state component styles |
| `.form__field-error` CSS class | `client/src/index.css` | CSS class | Inline validation error text |
| `.form__input--error` CSS class | `client/src/index.css` | CSS class | Input error outline |

<output>
Create `.planning/phases/02-creator-flow/02-02-SUMMARY.md` when done
</output>
